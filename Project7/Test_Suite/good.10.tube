# Make sure functions with no parameters can be called.  Also, use a global variable in a function.

int t = 1;
define int NoParameters() {
  t *= 5;
  return t + 5;
}

print NoParameters();
print NoParameters();
print NoParameters();
