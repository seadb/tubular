# A recursive way of summing up sequential ints (terrible algoithmically!)

define int Sum(int x) {
  if(x <= 0) {
    return 0;
  }
  return x + Sum(x-1);
}

int i = 0;
while (i < 25) {
  print Sum(i+=1);
}
